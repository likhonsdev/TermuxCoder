import React from 'react';
import ReactDOM from 'react-dom/client';
import TerminalIntro from './TerminalIntro.tsx';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <TerminalIntro />
  </React.StrictMode>,
)
